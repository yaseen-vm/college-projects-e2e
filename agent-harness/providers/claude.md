# Claude Adapter

Read the root `AGENTS.md` and the shared harness files before editing. Inspect first, use focused edits, and keep implementation decisions tied to the task brief. Run the shared verification steps and provide the standard handoff.

If the Claude environment supports project instructions, point them at `agent-harness/README.md`; this note complements, rather than replaces, that environment's safety rules.
